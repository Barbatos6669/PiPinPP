# PiPinPP v0.2.0 Release Notes

## 🎉 What's New in v0.2.0

This release transforms PiPinPP into a **complete, production-ready GPIO library** with full Arduino compatibility. Perfect for Arduino makers transitioning to Raspberry Pi!

### ✨ Major Features

#### 🔌 Enhanced Pin Class with Pull-Resistor Support
- **PinMode enum** with `INPUT`, `OUTPUT`, `INPUT_PULLUP`, `INPUT_PULLDOWN`
- **Internal pull-resistor configuration** via libgpiod bias flags
- **Pin number validation** with comprehensive error checking
- **Dual constructors** maintaining backward compatibility with v0.1.0

#### 🎯 Complete Arduino Compatibility Layer
Familiar Arduino functions are available:

```cpp
pinMode(17, OUTPUT);
digitalWrite(17, HIGH);
int value = digitalRead(18);
delay(1000);
```

#### 🧮 Arduino Math Functions
- `Arduino::constrain(value, min, max)` - Clamp values to range
- `Arduino::map(value, fromMin, fromMax, toMin, toMax)` - Scale values
- Namespaced to avoid conflicts with standard library

#### 📚 Four Complete Example Programs
1. `arduino_style/` - LED blinking with Arduino API
2. `basic_led/` - Object-oriented LED control with Pin class
3. `button_input/` - Button reading with debouncing and pull-ups
4. `arduino_migration/` - Side-by-side Arduino vs PiPinPP comparison

### 🛠️ Technical Improvements

- All examples build automatically with CMake
- Updated to v0.2.0 across all project files
- Enhanced pkg-config integration
- MIT license headers added to all source files

### 🧪 Hardware Validation

Tested on Raspberry Pi with real GPIO hardware. Pull-resistor functionality verified; all examples compile and run.

## 📋 How to Publish

1. Create a GitHub Release from tag `v0.2.0` (we created the tag). Use this file as release notes if you wish.
2. Add repository topics (raspberry-pi, gpio, arduino, cpp, libgpiod, embedded).
3. Wait 24–48 hours for GitHub search indexing to pick up changes.

---

For full details, see `CHANGELOG.md` and `docs/API_REFERENCE.md`.
